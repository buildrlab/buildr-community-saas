# Root env (optional). Prefer app-specific env files.
# Copy relevant entries into apps/web/.env.local and apps/api/.env

# Shared
AWS_REGION=us-east-1

# Enable test-mode auth bypass in local/dev only
ALLOW_TEST_MODE=false
TEST_USER=dev-user
