# URL of the API (local dev or production API Gateway)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Set to 'true' to enable test mode (bypasses Cognito auth)
# MUST be 'false' in production
NEXT_PUBLIC_TEST_MODE=false

# Test user ID when test mode is enabled (local dev only)
NEXT_PUBLIC_TEST_USER=
