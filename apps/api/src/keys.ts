export const userPk = (userId: string): string => `USER#${userId}`;
export const projectSk = (projectId: string): string => `PROJECT#${projectId}`;
export const workspacePk = (workspaceId: string): string => `WORKSPACE#${workspaceId}`;
export const memberSk = (userId: string): string => `MEMBER#${userId}`;
